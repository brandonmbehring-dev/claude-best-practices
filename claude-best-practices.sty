% claude-best-practices.sty
% Self-contained Tufte-style layout for the Claude Best Practices handbook.
% No external dependencies beyond standard TeX Live packages.
%
% Usage: \usepackage{claude-best-practices}
% Requires: LuaLaTeX with -shell-escape flag

\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{claude-best-practices}[2026/02/24 Claude Best Practices Style v2.0]

% =============================================================================
% ENGINE DETECTION
% =============================================================================
\RequirePackage{iftex}
\RequireLuaTeX

% =============================================================================
% TUFTE-STYLE GEOMETRY
% =============================================================================
\RequirePackage{geometry}
\geometry{
    paperwidth=8.5in,
    paperheight=11in,
    left=1in,
    top=1in,
    textwidth=4.5in,
    marginparsep=0.3in,
    marginparwidth=1.9in,
    textheight=9in,
    footskip=0.5in,
    headheight=23pt
}

% Full-width length for tables spanning text + margin
\newlength{\fullwidthlen}
\AtBeginDocument{\setlength{\fullwidthlen}{\dimexpr\textwidth+\marginparwidth+\marginparsep\relax}}

% =============================================================================
% FONTS
% =============================================================================
\RequirePackage{fontspec}

% Fallback font for symbols (stars, arrows, etc.)
\directlua{
  luaotfload.add_fallback("symbolfallback", {
    "DejaVu Sans:mode=harf;",
  })
}

\setmainfont{Roboto}[
  UprightFont = *-Regular,
  BoldFont = *-Bold,
  ItalicFont = *-Italic,
  BoldItalicFont = *-BoldItalic,
  Scale = 1.0,
  RawFeature={fallback=symbolfallback}
]

\setmonofont{Source Code Pro}[
  Scale = 0.82,
  UprightFont = *-Regular,
  BoldFont = *-Bold,
  ItalicFont = *-It,
  BoldItalicFont = *-BoldIt
]

\setsansfont{Roboto}[
  UprightFont = *-Regular,
  BoldFont = *-Bold,
  ItalicFont = *-Italic,
  BoldItalicFont = *-BoldItalic
]

\newcommand{\marginfontsize}{\footnotesize}

% =============================================================================
% TYPOGRAPHY
% =============================================================================
\RequirePackage[
  protrusion=true,
  expansion=true,
  tracking=smallcaps,
  final
]{microtype}

\SetProtrusion{encoding=*,family=*}{
  1={-100,-100},
  .={0,500},
  ,={0,500}
}

\RequirePackage{setspace}
\setstretch{1.15}

% Tufte-style: indent, no parskip
\setlength{\parindent}{1em}
\setlength{\parskip}{0pt}

% Typesetting safety: extra stretch for difficult line breaks,
% minor overflow tolerance (2pt is imperceptible),
% suppress underfull warnings from narrow table columns and URL breaks
\emergencystretch=1em
\hfuzz=2pt
\hbadness=10000
\vfuzz=2pt

% =============================================================================
% COLORS
% =============================================================================
\RequirePackage[dvipsnames,svgnames,x11names,table]{xcolor}

\definecolor{PrimaryBlue}{HTML}{0066CC}
\definecolor{NoteBlue}{HTML}{3498DB}
\definecolor{WarningOrange}{HTML}{E67E22}
\definecolor{TipGreen}{HTML}{27AE60}
\definecolor{CodeBg}{HTML}{F5F5F5}
\definecolor{CodeFrame}{HTML}{CCCCCC}
\definecolor{DarkText}{HTML}{222222}
\definecolor{OfficialPurple}{HTML}{7B2D8E}
\definecolor{PractitionerTeal}{HTML}{008080}
\definecolor{ConvergenceGold}{HTML}{B8860B}
\definecolor{EnterpriseNavy}{HTML}{003366}
\definecolor{CostRed}{HTML}{CC3333}
\definecolor{ExerciseIndigo}{HTML}{5B2C6F}
\definecolor{WhyAmber}{HTML}{D4AC0D}

% =============================================================================
% CODE LISTINGS (minted)
% =============================================================================
\RequirePackage{minted}

\setminted{
  frame=lines,
  framesep=1mm,
  bgcolor=CodeBg,
  rulecolor=CodeFrame,
  fontsize=\footnotesize,
  breaklines=true,
  breakanywhere=false,
  linenos=false,
  xleftmargin=0pt,
  baselinestretch=1.05,
  autogobble=true
}

\usemintedstyle{friendly}

\RequirePackage{listings}
\lstset{
  basicstyle=\ttfamily\footnotesize,
  keywordstyle=\color{PrimaryBlue},
  commentstyle=\color{gray},
  stringstyle=\color{WarningOrange},
  showstringspaces=false,
  breaklines=true,
  frame=single,
  numbers=none,
  tabsize=4,
  backgroundcolor=\color{CodeBg}
}

\RequirePackage[english]{babel}
\RequirePackage{csquotes}

% Inline code
\newcommand{\code}[1]{\texttt{#1}}

% =============================================================================
% MARGIN NOTES (Tufte-style)
% =============================================================================
\RequirePackage{changepage}
\RequirePackage{marginnote}

\let\pkgmarginnote\marginnote

% Flag: are we currently inside a margin note?
\newif\ifinmarginnote
\inmarginnotefalse

% Basic sidenote
\newcommand{\sidenote}[1]{%
  \pkgmarginnote{%
    \inmarginnotetrue
    \footnotesize\raggedright%
    \color{PrimaryBlue!70!black}%
    #1%
    \color{DarkText}%
    \inmarginnotefalse
    \vspace{2pt}%
  }%
}

% Full-width environment
\newenvironment{fullwidth}
  {\begin{adjustwidth}{}{-\dimexpr\marginparwidth+\marginparsep\relax}}
  {\end{adjustwidth}}

% =============================================================================
% CALLOUT BOXES (tcolorbox)
% =============================================================================
\RequirePackage[breakable,skins]{tcolorbox}

% --- MARGIN CALLOUT BOXES ---
\newtcolorbox{marginbox}[2][Note]{
  breakable=false,
  enhanced,
  colframe=#2,
  colback=#2!3,
  coltitle=DarkText,
  fonttitle=\bfseries\small,
  title=#1,
  leftrule=2pt,
  rightrule=0pt,
  toprule=0pt,
  bottomrule=0pt,
  arc=0pt,
  left=1pt,
  right=1pt,
  top=0.5pt,
  bottom=0.5pt,
  boxsep=0pt,
  width=\marginparwidth,
  fontupper=\marginfontsize\raggedright,
  titlerule=0mm
}

% Margin note with category
\renewcommand{\marginnote}[2][Note]{%
  \pkgmarginnote{%
    \inmarginnotetrue
    \begin{marginbox}[#1]{NoteBlue}#2\end{marginbox}%
    \inmarginnotefalse
    \vspace{2pt}%
  }%
}

\newcommand{\marginwarning}[2][Warning]{%
  \pkgmarginnote{%
    \inmarginnotetrue
    \begin{marginbox}[#1]{WarningOrange}#2\end{marginbox}%
    \inmarginnotefalse
    \vspace{2pt}%
  }%
}

\newcommand{\margintip}[2][Tip]{%
  \pkgmarginnote{%
    \inmarginnotetrue
    \begin{marginbox}[#1]{TipGreen}#2\end{marginbox}%
    \inmarginnotefalse
    \vspace{2pt}%
  }%
}

\newcommand{\marginofficial}[1]{%
  \pkgmarginnote{%
    \inmarginnotetrue
    \begin{marginbox}[Official]{OfficialPurple}#1\end{marginbox}%
    \inmarginnotefalse
    \vspace{2pt}%
  }%
}

\newcommand{\margincost}[1]{%
  \pkgmarginnote{%
    \inmarginnotetrue
    \begin{marginbox}[Cost]{CostRed}#1\end{marginbox}%
    \inmarginnotefalse
    \vspace{2pt}%
  }%
}

\newcommand{\marginenterprise}[1]{%
  \pkgmarginnote{%
    \inmarginnotetrue
    \begin{marginbox}[Enterprise]{EnterpriseNavy}#1\end{marginbox}%
    \inmarginnotefalse
    \vspace{2pt}%
  }%
}

\newcommand{\margintemplate}[1]{%
  \pkgmarginnote{%
    \inmarginnotetrue
    \begin{marginbox}[Template]{PractitionerTeal}#1\end{marginbox}%
    \inmarginnotefalse
    \vspace{2pt}%
  }%
}

% --- MAIN TEXT CALLOUT BOXES ---
\tcbset{
  callout-compact/.style={
    breakable,
    enhanced,
    arc=0pt,
    leftrule=3pt,
    rightrule=0pt,
    toprule=0pt,
    bottomrule=0pt,
    left=2pt,
    right=2pt,
    top=1pt,
    bottom=1pt,
    boxsep=0pt,
    toptitle=0.5pt,
    coltitle=DarkText,
    fonttitle=\bfseries
  }
}

\newtcolorbox{notebox}[1][Note]{
  callout-compact,
  colframe=NoteBlue,
  colback=NoteBlue!3,
  title=#1
}

\newtcolorbox{warnbox}[1][Warning]{
  callout-compact,
  colframe=WarningOrange,
  colback=WarningOrange!3,
  title=#1
}

\newtcolorbox{tipbox}[1][Tip]{
  callout-compact,
  colframe=TipGreen,
  colback=TipGreen!3,
  title=#1
}

\newtcolorbox{officialbox}[1][Anthropic Official]{
  callout-compact,
  colframe=OfficialPurple,
  colback=OfficialPurple!3,
  title=#1
}

% --- PEDAGOGICAL ENVIRONMENTS (v2.0) ---

% Before/After contrast box
\newtcolorbox{beforeafter}[1][Before \& After]{
  callout-compact,
  colframe=PrimaryBlue,
  colback=PrimaryBlue!3,
  title=#1
}
\newcommand{\before}{\par\textbf{Before:}\par}
\newcommand{\after}{\par\textbf{After:}\par}

% Try This — practice exercises (<10 min, reader's own project)
\newtcolorbox{trybox}[1][Try This]{
  callout-compact,
  colframe=ExerciseIndigo,
  colback=ExerciseIndigo!3,
  title=#1
}

% Why Box — reasoning/rationale (answers "why", not "what")
\newtcolorbox{whybox}[1][Why This Matters]{
  callout-compact,
  colframe=WhyAmber,
  colback=WhyAmber!3,
  title=#1
}

% Decision Box — decision framework (when to choose X over Y)
\newtcolorbox{decisionbox}[1][Decision Framework]{
  callout-compact,
  colframe=PractitionerTeal,
  colback=PractitionerTeal!3,
  title=#1
}

% Recovery — escape route from anti-pattern (pairs with \antipattern)
\newcommand{\recovery}[3]{%
  \begin{warnbox}[Recovery: #1]
    \textbf{You are here:} #2\par
    \textbf{Escape route:} #3
  \end{warnbox}
}

% Part Introduction — maturity context for each Part
\newtcolorbox{partintro}{
  breakable,
  enhanced,
  colframe=PrimaryBlue!50,
  colback=PrimaryBlue!3,
  arc=3pt,
  leftrule=0pt,
  rightrule=0pt,
  toprule=1pt,
  bottomrule=1pt,
  left=6pt,
  right=6pt,
  top=4pt,
  bottom=4pt,
  boxsep=0pt,
  fontupper=\itshape
}

% =============================================================================
% PRACTICE LABEL COMMANDS
% =============================================================================
% Inline labels for tagging practices
\newcommand{\official}[1]{%
  {\color{OfficialPurple}\textbf{[Official]}}~#1%
}
\newcommand{\practitioner}[1]{%
  {\color{PractitionerTeal}\textbf{[Practitioner]}}~#1%
}
\newcommand{\convergence}[1]{%
  {\color{ConvergenceGold}\textbf{[Convergence]}}~#1%
}

% =============================================================================
% TABLES
% =============================================================================
\RequirePackage{booktabs}
\RequirePackage{tabularx}
\RequirePackage{longtable}
\RequirePackage{array}
\RequirePackage{multicol}
\RequirePackage{multirow}
\renewcommand{\arraystretch}{1.2}

% =============================================================================
% MATH
% =============================================================================
\RequirePackage{amsmath,amssymb}

% =============================================================================
% FIGURES AND GRAPHICS
% =============================================================================
\RequirePackage{graphicx}
\RequirePackage{float}
\RequirePackage{caption}
\RequirePackage{tikz}
\usetikzlibrary{
  arrows.meta,
  positioning,
  shapes.geometric,
  shapes.misc,
  fit,
  calc,
  backgrounds,
  decorations.pathreplacing
}

\captionsetup{
  font=small,
  labelfont=bf,
  format=hang,
  margin=6pt,
  skip=4pt,
  singlelinecheck=false
}

% =============================================================================
% LISTS
% =============================================================================
\RequirePackage[shortlabels]{enumitem}

\setlist{
  topsep=0.5pt,
  itemsep=1.5pt,
  parsep=0pt,
  partopsep=0pt,
  leftmargin=*,
  listparindent=0pt
}

\setlist[description]{style=nextline}

% =============================================================================
% CROSS-REFERENCES AND HYPERLINKS
% =============================================================================
\RequirePackage[
  colorlinks=true,
  linkcolor=black,
  urlcolor=PrimaryBlue,
  citecolor=TipGreen!80!black,
  pdfusetitle,
  bookmarks=true,
  bookmarksnumbered=true,
  pdfstartview=FitH,
  pdfdisplaydoctitle=true,
  pdfencoding=unicode,
  hyperfootnotes=false
]{hyperref}

\RequirePackage[capitalise,noabbrev]{cleveref}

% Allow URL line breaks at any character (eliminates overfull in URLs)
\expandafter\def\expandafter\UrlBreaks\expandafter{\UrlBreaks
  \do\/\do\-\do\_\do\.\do\=\do\?\do\&\do\#\do\~\do\a\do\b\do\c\do\d
  \do\e\do\f\do\g\do\h\do\i\do\j\do\k\do\l\do\m\do\n\do\o\do\p\do\q
  \do\r\do\s\do\t\do\u\do\v\do\w\do\x\do\y\do\z\do\A\do\B\do\C\do\D
  \do\E\do\F\do\G\do\H\do\I\do\J\do\K\do\L\do\M\do\N\do\O\do\P\do\Q
  \do\R\do\S\do\T\do\U\do\V\do\W\do\X\do\Y\do\Z}

% =============================================================================
% HEADERS (scrlayer-scrpage)
% =============================================================================
\RequirePackage[headsepline]{scrlayer-scrpage}
\pagestyle{scrheadings}
% Guard: scrartcl has no \chapter
\@ifundefined{chapter}{\automark{section}}{\automark[chapter]{chapter}}

\providecommand{\volumeshort}{Claude Best Practices}

\ihead{\footnotesize\volumeshort}
\chead{}
\ohead{\footnotesize\textit{\leftmark}}
\ifoot{} \cfoot{\pagemark} \ofoot{}

% =============================================================================
% HEADING STYLES (KOMA-Script, Tufte italic)
% =============================================================================
\@ifundefined{chapter}{}{\addtokomafont{chapter}{\color{PrimaryBlue}\itshape}}
\addtokomafont{section}{\color{PrimaryBlue!90!black}\itshape}
\addtokomafont{subsection}{\color{PrimaryBlue!80!black}\itshape}

\@ifundefined{chapter}{}{%
  \RedeclareSectionCommand[
    beforeskip=1.0\baselineskip,
    afterskip=0.3\baselineskip
  ]{chapter}%
}

\RedeclareSectionCommand[
  beforeskip=0.6\baselineskip,
  afterskip=0.2\baselineskip
]{section}

\RedeclareSectionCommand[
  beforeskip=0.6\baselineskip,
  afterskip=0.15\baselineskip
]{subsection}

% =============================================================================
% DOCUMENT METADATA
% =============================================================================
\providecommand{\guidetitle}{Best Practices for Using Claude}
\providecommand{\guidesubtitle}{From Personal Mastery to Enterprise Scale}
\providecommand{\guideauthor}{Brandon Behring}
\providecommand{\guideversion}{2.0}
\providecommand{\guidedate}{February 2026}

\AtBeginDocument{%
  \hypersetup{
    pdftitle={\guidetitle},
    pdfauthor={\guideauthor},
    pdfsubject={Claude Code Best Practices},
    pdfkeywords={Claude Code, AI, LLM, best practices, 2026}
  }%
}

% =============================================================================
% COVER PAGE
% =============================================================================
\newcommand{\makeguidecover}{%
  \begin{titlepage}
    \centering
    \vspace*{2cm}

    {\Huge\bfseries\color{PrimaryBlue}\guidetitle\par}

    \vspace{0.5cm}
    {\Large\guidesubtitle\par}

    \vspace{0.3cm}
    {\normalsize\textit{What Anthropic Recommends, What Practitioners Discover,\\
    and Where They Converge}\par}

    \vspace{2cm}
    {\large\guideauthor\par}

    \vfill

    {\normalsize Version \guideversion\par}
    {\normalsize\guidedate\par}

    \vspace{0.5cm}
    {\footnotesize Content verified against Anthropic documentation as of \guidedate.\par}
    {\footnotesize Licensed under CC~BY~4.0\par}
  \end{titlepage}
}

% =============================================================================
% CONVENIENCE COMMANDS
% =============================================================================

% Key insight callout
\newcommand{\keyinsight}[1]{%
  \begin{notebox}[Key Insight]
    #1
  \end{notebox}
}

% Anti-pattern callout
\newcommand{\antipattern}[3]{%
  \begin{warnbox}[Anti-Pattern: #1]
    \textbf{Symptom:} #2\par
    \textbf{Prevention:} #3
  \end{warnbox}
}

% Maturity level indicator
\newcommand{\maturitylevel}[1]{%
  {\color{PrimaryBlue}\textbf{L#1}}%
}

% Source footnote (for Anthropic URLs)
% In margins: renders as tiny inline URL (footnotes fail inside hboxes)
% In body text: normal footnote
\newcommand{\sourceurl}[1]{%
  \ifinmarginnote
    \\[-2pt]{\tiny\raggedright\url{#1}\par}%
  \else
    \footnote{\url{#1}}%
  \fi
}

% =============================================================================
% END OF STYLE
% =============================================================================
\endinput
